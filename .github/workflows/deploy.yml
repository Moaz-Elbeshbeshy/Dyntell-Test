name: Deploy with Jenkins

on:
  push:
    branches:
      - ts

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins via webhook
        run: |
          curl -X POST "${{ secrets.JENKINS_URL }}/github-webhook/" || true
